using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using LDebugTool;


public class T2 : MonoBehaviour
{
    // Start is called before the first frame update
    [ContextMenu("Start")]
    void Start()
    {
        LDebug.Log("start 123");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
